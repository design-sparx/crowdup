import {MantineThemeOverride} from "@mantine/core";

export const customTheme: MantineThemeOverride = {
    primaryColor: 'green',
    primaryShade: 8,
    defaultRadius: 'sm',
    colorScheme: 'light'
}
